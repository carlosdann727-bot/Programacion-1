/*
Primera unidad: intro a c++, declaracion de variables y operaciones basicas, condicionales (if,else,else if,swicht,ternario)
Segunda unidad: bucles y arreglos
Tercera unlidad: Funciones  

*/

#include<iostream>
using namespace std;

int main(){
	
	
	//Programa: Determinar si un número es positivo, negativo o cero
	
/*	float n;
	cout<<"Ingrese un numero de su preferencia: ";
	cin>>n;
	
	if(n==0) 
	{
		cout<<"El numero que ingresaste es cero";
	} 
	else if(n<0)
	{
		cout<<"El numero ingresado es negativo";
	} 
	else
	{
	cout<<"El numero es positivo";	
	}
	
*/

//Programa: Calcular el descuento según el monto se aplica 10 % si el monto es mayor o igual a 100	
	
	/*float monto;
	cout<<"Ingresa el monto: ";
	cin>>monto;
	
	if(monto >= 100) 
	{
		float pdescuento = monto-(monto*0.10); 
		cout<<"El total a pagar es: "<<pdescuento<<"L";
	}
	else
	{
		cout<<"El total a pagar es: "<<monto<<"L";
	}
	*/
	
	
	//El usuario ingresa un número entero y el programa debe indicar si es par o impar usando la operación % y un if.
	
	return 0;
}



